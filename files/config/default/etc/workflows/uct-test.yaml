id: test
title: Test Encoding profiles
tags:
  - archive
operations:
  # Apply the encoding profiles ...
  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile A'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-a'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile B'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-b'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile C'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-c'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile D'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-d'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile E'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-e'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile F'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-f'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile G'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-g'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile H'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-h'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile I'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-i'
      - encoding-profile: uct-text-encoding

  - id: encode
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: 'Try encoding profile J'
    configurations:
      - source-flavor: '*/source'
      - target-flavor: '*/encoded-video-j'
      - encoding-profile: uct-text-encoding

  # Clean up work artifacts
  - id: cleanup
    fail-on-error: false
    description: Cleaning up - remove temp processing artifacts
    configurations:
      - delete-external: false
      # FixMe Don't clean up ACLs until workflow service no longer looks for them in the WFR.
      - preserve-flavors: dublincore/*,security/*
      - delay: 5
