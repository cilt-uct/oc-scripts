id: google-speech-attach-transcripts
title: Attach caption/transcripts generated by Google
tags: []
description: |-
  Attach transcription generated by the Google service.
      This is an internal workflow, started by the Transcription Service.
operations:
  # Check if event has been retracted
  - id: execute-once
    max-attempts: 3
    retry-strategy: hold
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: Check if event has been retracted
    configurations:
      - exec: /opt/opencast/wfexec/check-if-retracted.pl
      - load: '0.1'
      - params: '#{id} #{out}'
      - output-filename: retracted_event.txt
      - target-flavor: text/plain
      - set-workflow-properties: true
      - expected-type: Attachment
  # Attach caption/transcript
  - id: google-speech-attach-transcription
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: Attach captions/transcription
    configurations:
      # This is filled out by the transcription service when starting this workflow
      - transcription-job-id: ${transcriptionJobId}
      - line-size: 80
      # Change captions flavor from "captions/timedtext" to "captions/source"
      - target-flavor: captions/source
      - target-tag: engage-download
      - target-caption-format: vtt
      - target-element-type: track
  # Tag captions with laguage code
  - id: tag
    description: Tagging captions with language code
    configurations:
      - source-flavors: captions/source
      - target-tags: +lang:en,+generator:google-speech,+generator-type:auto
  # Merge caption/transcript to existing publication and republish
  - id: publish-engage
    fail-on-error: true
    if: NOT(${retracted_event})
    exception-handler-workflow: partial-error
    description: Distribute and publish to engage server
    configurations:
      - download-source-flavors: dublincore/*,security/*,captions/*
      - strategy: merge
      - check-availability: false
  - id: republish-oaipmh
    if: NOT(${retracted_event})
    exception-handler-workflow: partial-error
    description: Update recording metadata in default OAI-PMH repository
    configurations:
      - source-flavors: dublincore/*,security/*,captions/*
      - repository: default
  # Archive media package
  - id: snapshot
    fail-on-error: true
    exception-handler-workflow: partial-error
    description: Archive media package
    configurations:
      - source-flavors: '*/*'
  # Clean up work artifacts
  - id: cleanup
    fail-on-error: false
    description: Remove temporary processing artifacts
    configurations:
      - delete-external: true
      # FixMe Don't clean up ACLs until workflow service no longer looks for them in the WFR.
      - preserve-flavors: security/*
